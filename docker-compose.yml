version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5004:5000
    environment:
      - DB_NAME=dec-senator-trades-db
      - DB_USER=postgres
      - DB_PASSWORD=5hFGkqNitHKo
      - DB_PORT=5432
      - DB_HOST=dec-senator-trades-db.cjkcm6ky82v0.us-east-2.rds.amazonaws.com
      - RUN_ENV=TESTING

